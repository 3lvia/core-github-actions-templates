name: Unit Test
description: 'Run .NET unit tests.'
inputs:
  checkout:
    description: 'If `true`, the action will check out the repository. If `false`, the action will assume the repository has already been checked out.'
    required: false
    default: 'true'
  working-directory:
    description: 'Will run unit tests on projects under this working directory.'
    required: false
    default: './'
  dotnet-tool-manifest:
    description: |
      Path to the .NET tool manifest file, relative to the root of the repository.
      Only needed if you require .NET tools that are outside of `working-directory` for the build.
    required: false
    default: './.config/dotnet-tools.json'
  test-projects:
    description: 'Pattern to use to find test projects.'
    required: false
    default: 'unit*test*csproj'
  test-coverage:
    description: 'If test coverage should be computed. Requires that all test projects include the Nuget package coverlet.collector.'
    required: false
    default: 'false'
  coverlet-runsettings-file:
    description: |
      Path to a coverlet runsettings file, relative to the root of the repository.
      If not specified, no runsettings file will be used for coverlet.
    required: false
    default: ''
  test-results-artifact-name:
    description:
      'Name of the workflow artifact that the raw .trx test result files are uploaded to. Must be unique within the
      workflow run.'
    required: false
    default: 'unit-test-results-${{ github.job }}-${{ github.action }}'
  test-results-retention-days:
    description: 'How many days to keep the uploaded test result artifact.'
    required: false
    default: '14'
runs:
  using: 'composite'
  steps:
    - name: Checkout repository
      if: ${{ inputs.checkout == 'true' }}
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

    - name: Restore .NET tools
      shell: bash
      run: dotnet tool restore --tool-manifest "$DOTNET_TOOL_MANIFEST"
      env:
        DOTNET_TOOL_MANIFEST: ${{ inputs.dotnet-tool-manifest }}

    - name: Run unit tests
      shell: bash
      run: |
        testprojects=$(find . -ipath "*$TEST_PROJECTS")
        if [[ -z $testprojects ]]; then
            echo "Unable to find any dotnet projects matching the pattern $TEST_PROJECTS. Set the pattern using the parameter test-projects."
            exit 0 # TODO: Fail the action if no test projects are found
        else
            echo "Running dotnet test on test projects: $testprojects"
        fi

        for testproject in $testprojects; do
          # Try VSTest-compatible flags first.
          # Exit code 5 means the test host is running in Microsoft.Testing.Platform (MTP) mode
          # and rejected the VSTest-style flags as invalid — retry with MTP-native flags.
          # '|| exit_code=$?' prevents bash's set -e from aborting the script on non-zero exit.
          exit_code=0
          if [[ "$TEST_COVERAGE" == 'true' ]]; then
            if [[ "$COVERLET_RUNSETTINGS_FILE" != '' ]]; then
              dotnet test "$testproject" -l:trx --collect:"XPlat Code Coverage" --settings "$COVERLET_RUNSETTINGS_FILE" || exit_code=$?
            else
              dotnet test "$testproject" -l:trx --collect:"XPlat Code Coverage" || exit_code=$?
            fi
          else
            dotnet test "$testproject" -l:trx || exit_code=$?
          fi

          if [[ $exit_code -eq 5 ]]; then
            echo "VSTest flags not supported (exit code 5), retrying with Microsoft.Testing.Platform flags..."
            if [[ "$TEST_COVERAGE" == 'true' ]]; then
              if [[ "$COVERLET_RUNSETTINGS_FILE" != '' ]]; then
                echo "::warning::coverlet-runsettings-file is not supported in MTP mode. Ignoring '$COVERLET_RUNSETTINGS_FILE'. Use a testconfig.json in the test project instead. See https://github.com/coverlet-coverage/coverlet/blob/master/Documentation/Coverlet.MTP.Integration.md#configuration-files"
              fi
              dotnet test --project "$testproject" --report-trx --coverlet --coverlet-output-format cobertura
            else
              dotnet test --project "$testproject" --report-trx
            fi
          elif [[ $exit_code -ne 0 ]]; then
            exit $exit_code
          fi
        done
      env:
        TEST_PROJECTS: ${{ inputs.test-projects }}
        TEST_COVERAGE: ${{ inputs.test-coverage }}
        COVERLET_RUNSETTINGS_FILE: ${{ inputs.coverlet-runsettings-file }}
      working-directory: ${{ inputs.working-directory }}

    # The published check summary only carries outcomes; the raw .trx also has per-test timings,
    # start/end times and full failure details — and passed/skipped tests leave no other trace.
    # Without this upload the files die with the runner.
    - name: Upload test results
      if: always()
      continue-on-error: true
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: ${{ inputs.test-results-artifact-name }}
        path: |
          **/TestResults/*.trx
          .github/**/TestResults/*.trx
        retention-days: ${{ inputs.test-results-retention-days }}
        if-no-files-found: ignore

    - name: Publish test results
      uses: EnricoMi/publish-unit-test-result-action@d0a4676d0e0b938bc201470d88276b7c74c712b3 # v2.24.0
      if: always()
      with:
        files: |
          **/TestResults/*.trx
          .github/**/TestResults/*.trx
        check_name: 'Unit Test Results'

    - name: Combine Coverage Reports # This is because one report is produced per project, and we want one result for all of them.
      if: ${{ inputs.test-coverage == 'true' }}
      uses: danielpalme/ReportGenerator-GitHub-Action@d3ebf1f760f7d8ab92cc44d9bcfee7ad73722a31 # v5.5.11
      continue-on-error: true
      with:
        reports: '**/*.cobertura*.xml' # REQUIRED # Matches both VSTest (*.cobertura.xml) and MTP/coverlet.MTP (*.cobertura.{timestamp}.xml) output filenames.
        targetdir: '${{ github.workspace }}' # REQUIRED # The directory where the generated report should be saved.
        reporttypes: 'Cobertura' # The output formats and scope (separated by semicolon) Values: Badges, Clover, Cobertura, CsvSummary, Html, Html_Dark, Html_Light, Html_BlueRed, HtmlChart, HtmlInline, HtmlInline_AzurePipelines, HtmlInline_AzurePipelines_Dark, HtmlInline_AzurePipelines_Light, HtmlSummary, JsonSummary, Latex, LatexSummary, lcov, MarkdownSummary, MarkdownSummaryGithub, MarkdownDeltaSummary, MHtml, PngChart, SonarQube, TeamCitySummary, TextSummary, TextDeltaSummary, Xml, XmlSummary
        verbosity: 'Info' # The verbosity level of the log messages. Values: Verbose, Info, Warning, Error, Off
        title: 'Code Coverage' # Optional title.
        tag: '${{ github.run_number }}_${{ github.run_id }}' # Optional tag or build version.
        customSettings: '' # Optional custom settings (separated by semicolon). See: https://github.com/danielpalme/ReportGenerator/wiki/Settings.
        toolpath: 'reportgeneratortool' # Default directory for installing the dotnet tool.

    - name: Publish Code Coverage Report
      if: ${{ inputs.test-coverage == 'true' }}
      uses: irongut/CodeCoverageSummary@51cc3a756ddcd398d447c044c02cb6aa83fdae95 # v1.3.0
      continue-on-error: true
      with:
        filename: 'Cobertura.xml'
        badge: 'true'
        fail_below_min: 'false' # just informative for now
        format: 'markdown'
        hide_branch_rate: 'false'
        hide_complexity: 'false'
        indicators: 'true'
        output: 'both'
        thresholds: '50 75'

    - name: Add Coverage PR Comment
      if: ${{ inputs.test-coverage == 'true' && github.event_name == 'pull_request' }}
      uses: marocchino/sticky-pull-request-comment@5770ad5eb8f42dd2c4f34da00c94c5381e49af88 # v3.0.5
      continue-on-error: true
      with:
        recreate: 'true'
        path: 'code-coverage-results.md'
